// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: tenants.sql

package controlplanedb

import (
	"context"

	"github.com/google/uuid"
)

const countTenantsByStatus = `-- name: CountTenantsByStatus :many
SELECT status, count(*)::bigint AS count
FROM tenants
GROUP BY status
ORDER BY status
`

type CountTenantsByStatusRow struct {
	Status string
	Count  int64
}

func (q *Queries) CountTenantsByStatus(ctx context.Context) ([]CountTenantsByStatusRow, error) {
	rows, err := q.db.Query(ctx, countTenantsByStatus)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []CountTenantsByStatusRow
	for rows.Next() {
		var i CountTenantsByStatusRow
		if err := rows.Scan(&i.Status, &i.Count); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const createTenant = `-- name: CreateTenant :one
INSERT INTO tenants (name, slug, db_name, status, plan)
VALUES ($1, $2, $3, $4, $5)
RETURNING id, name, slug, db_name, status, plan, created_at, updated_at
`

type CreateTenantParams struct {
	Name   string
	Slug   string
	DbName string
	Status string
	Plan   string
}

func (q *Queries) CreateTenant(ctx context.Context, arg CreateTenantParams) (Tenant, error) {
	row := q.db.QueryRow(ctx, createTenant,
		arg.Name,
		arg.Slug,
		arg.DbName,
		arg.Status,
		arg.Plan,
	)
	var i Tenant
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Slug,
		&i.DbName,
		&i.Status,
		&i.Plan,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteTenant = `-- name: DeleteTenant :exec
DELETE FROM tenants WHERE id = $1
`

func (q *Queries) DeleteTenant(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, deleteTenant, id)
	return err
}

const getTenantByID = `-- name: GetTenantByID :one
SELECT id, name, slug, db_name, status, plan, created_at, updated_at FROM tenants WHERE id = $1
`

func (q *Queries) GetTenantByID(ctx context.Context, id uuid.UUID) (Tenant, error) {
	row := q.db.QueryRow(ctx, getTenantByID, id)
	var i Tenant
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Slug,
		&i.DbName,
		&i.Status,
		&i.Plan,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getTenantBySlug = `-- name: GetTenantBySlug :one
SELECT id, name, slug, db_name, status, plan, created_at, updated_at FROM tenants WHERE slug = $1
`

func (q *Queries) GetTenantBySlug(ctx context.Context, slug string) (Tenant, error) {
	row := q.db.QueryRow(ctx, getTenantBySlug, slug)
	var i Tenant
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Slug,
		&i.DbName,
		&i.Status,
		&i.Plan,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listTenants = `-- name: ListTenants :many
SELECT id, name, slug, db_name, status, plan, created_at, updated_at FROM tenants ORDER BY created_at
`

func (q *Queries) ListTenants(ctx context.Context) ([]Tenant, error) {
	rows, err := q.db.Query(ctx, listTenants)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Tenant
	for rows.Next() {
		var i Tenant
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Slug,
			&i.DbName,
			&i.Status,
			&i.Plan,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const setTenantStatus = `-- name: SetTenantStatus :one
UPDATE tenants SET status = $2 WHERE id = $1
RETURNING id, name, slug, db_name, status, plan, created_at, updated_at
`

type SetTenantStatusParams struct {
	ID     uuid.UUID
	Status string
}

func (q *Queries) SetTenantStatus(ctx context.Context, arg SetTenantStatusParams) (Tenant, error) {
	row := q.db.QueryRow(ctx, setTenantStatus, arg.ID, arg.Status)
	var i Tenant
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Slug,
		&i.DbName,
		&i.Status,
		&i.Plan,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const updateTenantPlatform = `-- name: UpdateTenantPlatform :one
UPDATE tenants
SET
    name = COALESCE($1, name),
    status = COALESCE($2, status),
    plan = COALESCE($3, plan)
WHERE id = $4
RETURNING id, name, slug, db_name, status, plan, created_at, updated_at
`

type UpdateTenantPlatformParams struct {
	Name   *string
	Status *string
	Plan   *string
	ID     uuid.UUID
}

func (q *Queries) UpdateTenantPlatform(ctx context.Context, arg UpdateTenantPlatformParams) (Tenant, error) {
	row := q.db.QueryRow(ctx, updateTenantPlatform,
		arg.Name,
		arg.Status,
		arg.Plan,
		arg.ID,
	)
	var i Tenant
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Slug,
		&i.DbName,
		&i.Status,
		&i.Plan,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
